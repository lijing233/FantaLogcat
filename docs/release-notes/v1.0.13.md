# FantaLogcat 1.0.13

## 设置体验

- 设置从浮层弹窗改为应用内独立页面，避免内容过长、靠下显示和遮挡主界面。
- 新增通用、日志与导出、软件更新、关于四个分类；左侧切换分类，右侧滚动查看内容。
- 顶部提供返回与保存操作；未保存时返回会丢弃草稿，外观预览也会恢复。

## 更新

- 保留每次启动时的后台更新检查，以及手动检查、自动下载和“安装并重启应用”流程。
- 设置页打开时安装更新会先回到主界面，再交由 Sparkle 退出、替换并重新启动应用。

## 验证

- 通过 Release 构建与 123 个单元测试。
